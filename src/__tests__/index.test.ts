/* eslint-env jest */

// This is here for now so jest doesn't complain
// that there are 0 tests and exits
// remove when we have our first unit test
describe('Basic test', () => {
  it('should be written', () => {})
})
